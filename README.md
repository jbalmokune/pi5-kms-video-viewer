# pi5-kms-video-viewer
A lightweight SDL2 middleware tool for the Raspberry Pi 5 (Bookworm and later) that reads raw BGRA frames from FFmpeg via stdin and displays them fullscreen using the modern KMS/DRM stack, completely bypassing the deprecated fb0 system.
